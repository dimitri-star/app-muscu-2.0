# FitTrack Pro - app mobile (Expo web export)
[build]
  command = "npm run build:web"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA routing : toutes les URLs → index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
