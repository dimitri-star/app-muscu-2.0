# FitTrack Pro - app web (Next.js dans /web)
[build]
  base = "web"
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

# Plugin Next.js pour le routing et le SSR
[[plugins]]
  package = "@netlify/plugin-nextjs"
